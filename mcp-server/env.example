# =============================================================================
# MCP Voice TTS Configuration
# =============================================================================
# Copy this file to .env and customize as needed.
# All settings have sensible defaults; only override what you need.
#
# Environment variables take precedence over .env file values.
# =============================================================================

# =============================================================================
# TTS SERVER CONNECTION
# =============================================================================
# URL of the TTS server (Docker container running on desktop machine)
# TTS_SERVER_URL=http://10.111.79.180:8080

# Request timeout in seconds for TTS synthesis
# Increase for longer text or slower networks
# REQUEST_TIMEOUT=60

# =============================================================================
# VOICE SETTINGS
# =============================================================================
# Default voice reference filename (must exist on TTS server)
# DEFAULT_VOICE=voice_reference.wav

# Default language code for synthesis
# Supported: en, es, fr, de, it, pt, pl, tr, ru, nl, cs, ar, zh, ja, hu, ko, hi
# DEFAULT_LANGUAGE=en
