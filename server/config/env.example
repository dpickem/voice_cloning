# =============================================================================
# TTS Server Environment Configuration
# =============================================================================
# Copy this file to .env and customize as needed.
# All settings have sensible defaults; only override what you need.
#
# Environment variables take precedence over .env file values.
# =============================================================================

# =============================================================================
# DIRECTORY PATHS
# =============================================================================
# Directory containing voice reference WAV files for voice cloning
# Default: <project_root>/data/voice_references
# VOICE_REFERENCES_DIR=

# Directory for saving generated audio files
# Default: <server_dir>/audio_output
# AUDIO_OUTPUT_DIR=

# Directory containing fine-tuned model files
# Default: <server_dir>/finetuned_model
# FINETUNED_MODEL_DIR=

# =============================================================================
# VOICE SETTINGS
# =============================================================================
# Default voice reference filename (must exist in VOICE_REFERENCES_DIR)
# DEFAULT_VOICE=voice_reference.wav

# =============================================================================
# SERVER SETTINGS
# =============================================================================
# Server bind address
# Use 0.0.0.0 for all interfaces (Docker, production)
# Use 127.0.0.1 for localhost only (development)
# HOST=0.0.0.0

# Server port number
# PORT=8080

# =============================================================================
# MODEL SETTINGS
# =============================================================================
# Model identifier for zero-shot TTS (Coqui TTS model path)
# TTS_MODEL=tts_models/multilingual/multi-dataset/xtts_v2

# Default audio sample rate in Hz
# DEFAULT_SAMPLE_RATE=22050
